schema_version: 1
setups:
  virtual_wealth:
    markets: [virtual_robinhood, virtual_base]
research:
  virtual_cross_chain:
    run_id: virtual-wealth-live
    setup: virtual_wealth
    inventory_mode: prepositioned
    price_source: weth_usd
    fixed_cost: {asset: usd, amount: "0.5"}
    min_net_profit: "0"
    sizing: {kind: linear_range, min: "100", max: "5000", samples: 10}
